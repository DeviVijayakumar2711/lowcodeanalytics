streamlit==1.32.2
pandas>=1.5.0
numpy>=1.23.0
requests>=2.28.0
prophet>=1.1.1          # Facebook/Meta’s Prophet time-series library
scikit-learn>=1.1.3
pyod>=1.0.6             # for IForest anomaly detection
seaborn>=0.12.0
matplotlib>=3.6.0
cmdstanpy>=1.0.8        # Prophet’s default backend
dowhy>=0.8.1            # optional causal-inference (can omit if you don’t need it)
graphviz>=0.20.1        # for CausalModel.view_model() (optional)
